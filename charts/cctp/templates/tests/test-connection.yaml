apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cctp.fullname" . }}-test-connection"
  labels:
    {{- include "cctp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cctp.fullname" . }}:{{ .Values.service.port }}/health-check']
  restartPolicy: Never

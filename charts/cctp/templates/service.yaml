apiVersion: v1
kind: Service
metadata:
  name: {{ include "cctp.fullname" . }}
  labels:
    {{- include "cctp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cctp.selectorLabels" . | nindent 4 }}

/**
 * Validates an address and returns the parsed (checksummed) version of that address
 *
 * @param address the unchecksummed hex address
 */
export declare const validateAndParseAddress: (address: string) => string;
